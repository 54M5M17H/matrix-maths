machine:
	node:
		version: 8

dependencies:
	pre:
		- echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
	override:
		- npm install

test:
	override: 
		npm t

deployment:
	production:
		branch: master
		commands:
			- npm version patch
			- npm publish