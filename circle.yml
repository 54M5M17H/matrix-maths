machine:
    node:
        version: 8

dependencies:
    override:
        - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
        - npm install

test:
    override:
        - npm test
        - npm lint

deployment:
    production:
        branch: master
        commands:
            - npm version patch
            - npm publish